node_modules
dist
dist-ssr
*.local
.vite/

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Lockfile — committed only if the project switches to a lockfile-pinned workflow.
package-lock.json

# Test fixtures (binary). Descriptor .txt files are still committed;
# CI downloads fixtures separately. See docs/CleanLabel_fixtures.md.
fixtures/**/*.jpg
fixtures/**/*.jpeg
fixtures/**/*.png
fixtures/**/*.webp
fixtures/**/*.heic
fixtures/**/*.tiff

# On-device inpainting model. ~50 MB. Downloaded on first dev-server
# run. Do not commit.
lama-model.onnx
models/

# Test artifacts
coverage/
.nyc_output/
.vitest-cache/

docs/
legacy/
post/